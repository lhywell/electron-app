/**
 * 应用配置文件
 */
export const appConfig = {
  
  // 应用描述
  description: '基于 Vue 3 + TypeScript + Ant Design Vue + Electron 构建的桌面应用',
  
  // 公司/组织名称
  company: '',
  
  // 其他配置项
  theme: {
    primaryColor: '#1890ff',
    layout: 'side', // side | top | mix
  },
  
  // 窗口配置
  window: {
    width: 1200,
    height: 800,
    minWidth: 800,
    minHeight: 600,
  }
}

export default appConfig
